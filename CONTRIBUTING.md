# Contributing to Clawstrider

1. Fork the repo.
2. Create a feature branch: `git checkout -b feature/amazing-logic`.
3. Install dev dependencies: `pip install -e .[dev]`.
4. Run tests: `pytest`.
5. Submit a PR.

Please ensure all new features include unit tests and docstrings.
